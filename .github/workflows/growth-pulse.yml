name: Scheduled SDK Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: scheduled-vote-battle-sdk-health-check
  cancel-in-progress: true

jobs:
  scheduled-verify:
    name: Verify vote-battle-sdk from npm
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare clean environment
        run: mkdir -p /tmp/vote-battle-sdk-check

      - name: Initialize project
        working-directory: /tmp/vote-battle-sdk-check
        run: npm init -y

      - name: Fetch vote-battle-sdk from registry
        working-directory: /tmp/vote-battle-sdk-check
        run: npm install vote-battle-sdk

      - name: Confirm SDK require succeeds
        working-directory: /tmp/vote-battle-sdk-check
        run: node -e "const sdk = require('vote-battle-sdk'); console.log('Scheduled health check: vote-battle-sdk OK');"

      - name: Show dependency tree
        working-directory: /tmp/vote-battle-sdk-check
        run: npm ls --all
